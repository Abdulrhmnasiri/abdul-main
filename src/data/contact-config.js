// Web3Forms (https://web3forms.com) delivers the contact form directly to
// the founder's inbox with no custom backend. The Access Key below is
// designed for static frontend use (it is not a private backend secret),
// but it must still be the founder's own key — do not invent one here.
// Leave web3FormsAccessKey empty until the founder provides it; the form
// will show its calm failure message to visitors until then.
export const contactConfig = {
  endpoint: 'https://api.web3forms.com/submit',
  web3FormsAccessKey: '9ddad33b-4851-4359-b037-1543ec16f62e',
};
